<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Appointment.Views.AppointmentMain">
  
  <StackLayout>
    <Label Text="Select Option" VerticalOptions="Center" HorizontalOptions="Center" />
    <Button Text="{Binding SelectDoctorLabel}" Command="{Binding SelectDoctorCommand}"/>
    <Button Text="{Binding SelectTimeLabel}" Command="{Binding SelectTimeCommand}"/>
  </StackLayout>
</ContentPage>